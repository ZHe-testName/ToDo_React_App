import React from 'react';

//Used span for macking component more independence
//and used it not only in ul(if it will be need)
const ToDoListItem = () => <span>Drink Coffe</span>;

export default ToDoListItem;